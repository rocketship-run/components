---
interface Props {
  class?: string;
}

const { class: customClass = "" } = Astro.props;
---

<h2 class={`card-title ${customClass}`}>
  <slot />
</h2>

<style>
  .card-title {
    margin: 0;
    font-size: 1.5rem;
    font-weight: 600;
    color: hsl(var(--card-foreground));
  }
</style>